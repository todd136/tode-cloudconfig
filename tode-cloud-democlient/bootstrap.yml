spring:
  application:
    name: tode-cloud-democlient
#  cloud:
#    config:
#      uri: ${CONFIG_SERVER_URI:${vcap.services.${PREFIX:}configserver.credentials.uri:http://192.168.1.67:${config.port:8888}}}
